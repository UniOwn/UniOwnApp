interface IRoutes {
    home: string;
    profile: string;
    passport: string;
}

const routes: IRoutes = {
    home: "/",
    profile: "/profile",
    passport: "/passport"
};

export default routes;
